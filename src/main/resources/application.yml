spring:
  datasource:
    url: jdbc:postgresql://localhost:55433/dealership
    username: dealership
    password: dealership

  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate
logging:
  level:
    org.flywaydb: DEBUG
    org.springframework.jdbc.datasource: DEBUG