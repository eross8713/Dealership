spring:
  datasource:
    url: jdbc:postgresql://localhost:55433/dealership
    username: dealership
    password: dealership

  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: localhost
      port: 6379
logging:
  level:
    org.flywaydb: DEBUG
    org.springframework.jdbc.datasource: DEBUG